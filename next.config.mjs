/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "img.etimg.com",
          "images.pexels.com",
          "assets.vogue.com",
          "m.media-amazon.com",
          "upload.wikimedia.org",
        ],
      },
};

export default nextConfig;
